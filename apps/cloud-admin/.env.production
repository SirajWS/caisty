# Öffentliche API-URL für den Admin-Build (Vite bakes this in at build time).
# Ohne diese Variable zeigen JSON-Requests zwar ggf. Daten, aber direkte fetch()-URLs
# auf admin.* würden 404 liefern — siehe fetchAdminInvoiceHtml / API_BASE in src/lib/api.ts
VITE_API_BASE_URL=https://api.caisty.com
